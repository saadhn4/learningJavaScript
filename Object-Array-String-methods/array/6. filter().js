//filter()

//this method creates a new array with all element that pass a test (provided as a function)

//filters elements based on conditions , returning a new array with element that satisfy the condition

let number = [1, 2, 3, 4, 5, 6];
let check = number.filter((x) => x > 3);
console.log(check);
